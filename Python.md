## 变量和简单数据类型

### 变量

- 每个变量都指向一个值（相关联的信息）。
- 在程序中可随时修改变量的值，Python 始终记录变量的最新值。
- 不需要规定变量的类型。
- 变量并不直接储存值，而是存储了值的内存地址或引用。
- 允许多个变量指向同一个值的地址。
- Python 采用基于值的内存管理方式：如果将不同变量赋给相同的值，这个值在内存中只有一份，即多个变量指向同一个内存地址（当对象非常大时，这种共享引用更有意义）。

#### 变量的命名和使用规则

- 变量名只能包含**字母、数字和下划线**，可以字母或下划线开头，**不能以数字开头**。
- 变量名**不能包含空格**，但可使用下划线分隔单词。
- **不要将 Python 关键字和函数名用作变量名**（如 `print`）。
- 变量名应**既简短又具有描述性**（如 `name` 比 `n` 好）。
- 慎用小写字母 `l` 和大写字母 `O`，因为可能被错看成数字 `1` 和 `0`。

### 数据类型

> 分为不可变类型、可变类型。如数字、字符串、布尔、元组、列表、字典、集合。

#### 数字

#### 整数

- 支持运算：`+` `-` `*` `/`
- 乘方：`**`（如 `3 ** 2` 得 9）
- 支持运算次序，可使用圆括号修改

#### 浮点数

- Python 将所有带小数点的数称为**浮点数**。
- 结果包含的小数位数可能不确定（如 `0.2 + 0.1` 得到 `0.30000000000000004`），这是所有语言都存在的问题。

> 任意两个数相除，结果总是**浮点数**。

> 只要有一个操作数是浮点数，结果总是浮点数。

#### 数中的下划线

- 书写大数时，可使用下划线分组（如 `14_000_000_000`）。
- Python 存储时会忽略下划线，打印时不会显示。
- 仅 Python 3.6 及更高版本支持。

#### 字符串

- 字符串就是**一系列字符**。
- 用**引号**括起的都是字符串，可以是单引号、双引号。
- 灵活性：可在字符串中包含引号和撇号，但注意不要与表示字符串的引号混淆。
- 字符串是字符的序列，可以按照单个字符或字符片段进行索引。字符串包括两种符号体系：
    - 正向递增序号（最左侧字符序号为 0）
    - 反向递减序号（最右侧字符序号为 -1）
- 字符串也提供区间访问方式，采用 `[N:M]` 格式，表示字符串中从 N 到 M（不包含 M）的子字符串；N 和 M 可混合使用正向和反向序号。

#### 修改字符串的大小写

- `title()`：以首字母大写方式显示_每个单词_。
- `upper()`：将字符串全部改为大写。
- `lower()`：将字符串全部改为小写。
- 存储数据时常用 `lower()` 来规范化用户输入。

#### f 字符串（在字符串中使用变量）

- **f 字符串**（Python 3.6 引入）：在字符串前写 `f` / `F`，用 `{变量/表达式}` 把变量直接嵌入字符串。
- 示例：`f"{first_name} {last_name}"`
- 可在花括号内调用方法，如：`f"{full_name.title()}"`
- 对于 Python 3.5 及更早版本，使用 `format()` 方法。

#### 使用制表符或换行符来添加空白

- `\t`：添加**制表符**。
- `\n`：添加**换行符**。
- 可组合使用，如：`"\n\t"`。

#### 删除空白

- `rstrip()`：删除字符串**末尾**的空白（临时删除，需重新赋值才永久生效）。
- `lstrip()`：删除字符串**开头**的空白。
- `strip()`：删除字符串**两端**的空白。
- 常用于在存储用户输入前进行清理。

### 赋值语句

- 执行逻辑：
    1. 优先计算等号右侧的所有表达式；
    2. 将右侧内存对象的引用，绑定给左侧变量。
- 变量无数据类型，仅仅是内存对象的「标签」。
- 数据类型、数值、内存空间都属于右侧对象。
- 变量可以反复绑定不同类型、不同数值的对象。

#### 基础赋值

- 语法格式：`变量名 = 数据 / 表达式`
- 左侧只能是变量，禁止常量、表达式、数字。

#### 多变量赋值

1. **链式赋值**：多个变量绑定同一个内存对象
    
    `a = b = c = 100`
    
    执行顺序：`c=100 → b=c → a=b`（a、b、c 指向同一个对象）
    
2. **解包赋值（序列解包）**：适用于元组、列表、字符串等可迭代对象
    
    标准解包（数量一一对应）：
    
    - `x, y = 10, 20`
    - `m, n = [1, 2]`
    
    经典用法：两变量交换（无需临时变量）：
    
    - `a, b = b, a`
3. **星号 `*` 多值接收**：用于接收剩余所有元素，适配元素数量不匹配场景
    
    `num, *rest = [1, 2, 3, 4, 5]`
    
    `num = 1`，`rest = [2, 3, 4, 5]`
    
4. **增量赋值运算符**
    
    全部运算符：`+= -= *= /= //= %= **=`
    
    语法等价关系：
    
    - `a += 1` 等价于 `a = a + 1`
    - `x *= 3` 等价于 `x = x * 3`
    
    > 关键深浅区别：
    
    > 1. 不可变对象（int/str/tuple）使用 `+=` 会创建新对象，变量重新绑定地址；
    
    > 2. 可变对象（list/字典/集合）使用 `+=` 通常原地修改原对象，不创建新对象。
    
5. **海象运算符 `:=`（Python 3.8+）**
    

- 核心作用：赋值 + 返回值，可将赋值操作嵌入 `if/while` 条件表达式。赋值后返回数值，可直接写在判断语句中。
- 实用示例（循环读取输入，输入 quit 则退出）：

```python
while (line := input("请输入：")) != "quit":
    print("输入内容：", line)
```

### 注释

#### 如何编写注释

- 单行注释用**井号（`#`）**标识，井号后面的内容被 Python 解释器忽略。
- 多行注释用三引号引起。

---

## math 库

### math 库的数学常数（共 4 个）

|常数|数学表示|描述|
|---|---|---|
|`math.pi`|$\pi$|圆周率，值为 3.141 592 653 589 793|
|`math.e`|$\mathrm{e}$|自然对数，值为 2.718 281 828 459 045|
|`math.inf`|$\infty$|正无穷大，负无穷大为 `-math.inf`|
|`math.nan`|$\mathrm{NaN}$|非浮点数标记，NaN（Not a Number）|

### math 库的数值表示函数（共 16 个）

|函数|数学表示|描述|
|---|---|---|
|`math.fabs(x)`|$\|x\|
|`math.fmod(x, y)`|$x \% y$|返回 $x$ 与 $y$ 的模|
|`math.fsum([x, y, ...])`|$x+y+\dots$|浮点数精确求和|
|`math.ceil(x)`|$\lceil x\rceil$|向上取整，返回不小于 $x$ 的最小整数|
|`math.floor(x)`|$\lfloor x\rfloor$|向下取整，返回不大于 $x$ 的最大整数|
|`math.factorial(x)`|$x!$|返回 $x$ 的阶乘；如果 $x$ 是小数或负数，抛出 `ValueError`|
|`math.gcd(a, b)`|—|返回 $a$ 与 $b$ 的最大公约数|
|`math.frexp(x)`|$x=m\times2^e$|返回 $(m, e)$；当 $x=0$ 时，返回 `(0.0, 0)`|
|`math.ldexp(x, i)`|$x\times2^i$|返回 $x\times2^i$；`math.frexp(x)` 的反运算|
|`math.modf(x)`|—|返回 $x$ 的小数和整数部分|
|`math.trunc(x)`|—|返回 $x$ 的整数部分|
|`math.copysign(x, y)`|$\|x\|
|`math.isclose(a, b)`|—|比较 a 和 b 的相似性，返回 `True` 或 `False`|
|`math.isfinite(x)`|—|当 $x$ 为无穷大，返回 `True`；否则，返回 `False`|
|`math.isinf(x)`|—|当 $x$ 为正数或负数无穷大，返回 `True`；否则，返回 `False`|
|`math.isnan(x)`|—|当 $x$ 是 NaN，返回 `True`；否则，返回 `False`|

### math 库的幂对数函数（共 8 个）

|函数|数学表示|描述|
|---|---|---|
|`math.pow(x, y)`|$x^y$|返回 $x$ 的 $y$ 次幂|
|`math.exp(x)`|$\mathrm{e}^x$|返回 $\mathrm{e}$ 的 $x$ 次幂（$\mathrm{e}$ 是自然对数）|
|`math.expm1(x)`|$\mathrm{e}^x-1$|返回 $\mathrm{e}$ 的 $x$ 次幂减 1|
|`math.sqrt(x)`|$\sqrt{x}$|返回 $x$ 的平方根|
|`math.log(x[, base])`|$\log_{base}x$|返回 $x$ 的对数；仅输入 $x$ 时返回自然对数 $\ln x$|
|`math.log1p(x)`|$\ln(1+x)$|返回 $1+x$ 的自然对数|
|`math.log2(x)`|$\log_2 x$|返回 $x$ 的以 2 为底对数|
|`math.log10(x)`|$\log_{10}x$|返回 $x$ 的以 10 为底对数|

### math 库的三角运算函数（共 16 个）

|函数|数学表示|描述|
|---|---|---|
|`math.degrees(x)`|—|弧度值 $x$ 转角度值|
|`math.radians(x)`|—|角度值 $x$ 转弧度值|
|`math.hypot(x, y)`|$\sqrt{x^2+y^2}$|返回坐标 $(x, y)$ 到原点 $(0,0)$ 的距离|
|`math.sin(x)`|$\sin x$|返回 $x$ 的正弦函数值（$x$ 为弧度）|
|`math.cos(x)`|$\cos x$|返回 $x$ 的余弦函数值（$x$ 为弧度）|
|`math.tan(x)`|$\tan x$|返回 $x$ 的正切函数值（$x$ 为弧度）|
|`math.asin(x)`|$\arcsin x$|返回 $x$ 的反正弦函数值（$x$ 为弧度）|
|`math.acos(x)`|$\arccos x$|返回 $x$ 的反余弦函数值（$x$ 为弧度）|
|`math.atan(x)`|$\arctan x$|返回 $x$ 的反正切函数值（$x$ 为弧度）|
|`math.atan2(y, x)`|$\arctan(y/x)$|返回 $y/x$ 的反正切函数值（结果为弧度）|
|`math.sinh(x)`|$\sinh x$|返回 $x$ 的双曲正弦函数值|
|`math.cosh(x)`|$\cosh x$|返回 $x$ 的双曲余弦函数值|
|`math.tanh(x)`|$\tanh x$|返回 $x$ 的双曲正切函数值|
|`math.asinh(x)`|$\operatorname{arcsinh}x$|返回 $x$ 的反双曲正弦函数值|
|`math.acosh(x)`|$\operatorname{arccosh}x$|返回 $x$ 的反双曲余弦函数值|
|`math.atanh(x)`|$\operatorname{arctanh}x$|返回 $x$ 的反双曲正切函数值|

### math 库的高等特殊函数（共 4 个）

|函数|数学表示|描述|
|---|---|---|
|`math.erf(x)`|$\frac{2}{\sqrt{\pi}}\int_{0}^{x}\mathrm{e}^{-t^2}\mathrm{d}t$|高斯误差函数，应用于概率论、统计学等领域|
|`math.erfc(x)`|$\frac{2}{\sqrt{\pi}}\int_{x}^{\infty}\mathrm{e}^{-t^2}\mathrm{{d}}t$|余补高斯误差函数，`math.erfc(x)=1-math.erf(x)`|
|`math.gamma(x)`|$\int_{0}^{\infty}t^{x-1}\mathrm{e}^{-t}\mathrm{d}t$|伽玛（Gamma）函数，也叫欧拉第二积分函数|
|`math.lgamma(x)`|$\ln(\mathrm{gamma}(x))$|伽玛函数的自然对数|

## 列表简介

### 列表是什么

- 列表由一系列按特定顺序排列的元素组成。
- 用方括号（`[]`）表示列表，用逗号分隔元素。
- 列表通常包含多个元素，建议使用复数名称（如 `names`）。

### 访问列表元素

- 通过索引访问：`list_name[index]`
- 索引从 **0** 开始，而不是 1。
- 访问最后一个元素可使用索引 `-1`，倒数第二个用 `-2`，依此类推。
- 可以对列表元素调用字符串方法，如：`bicycles[0].title()`

### 使用列表中的各个值

- 可以像使用普通变量一样使用列表中的值。
- 可在 f 字符串中嵌入列表元素。

### 修改、添加和删除元素

#### 修改列表元素

- 直接通过索引赋值：`list_name[0] = 'new_value'`

#### 在列表中添加元素

- `append(item)`：在**末尾**添加元素。
- `insert(index, item)`：在**指定位置**插入元素，其他元素右移。

#### 从列表中删除元素

- `del list_name[index]`：删除指定位置的元素（不可再访问，无法得到删除的数据）。
    
- `pop()`：删除**末尾**元素并_返回该值_（可继续使用，可以拿到删除的具体数据）。
    
- `pop(index)`：删除**指定位置**元素并返回该值。
    
- `remove(value)`：根据**值**删除第一个匹配的元素。
    
    注意：`remove()` 只删除第一个匹配项，若要删除所有匹配项需使用循环（第 7 章）。
    

### 组织列表

#### 方法 `sort()` 对列表永久排序

- `list.sort()`：按字母顺序永久排序。
- `list.sort(reverse=True)`：按字母逆序永久排序。

#### 函数 `sorted()` 对列表临时排序

- `sorted(list)`：返回排序后的新列表，从小到大，原列表不变。
- `sorted(list, reverse=True)`：按逆序临时排序。

> 返回值是排序后的列表，需要用变量接住返回的新列表，不能单独使用。

> `.sort()` 是列表方法，直接把原列表改了，返回 `None`。

> 字符串也可以排序（按字符编码）。

#### 倒着打印列表

- `reverse()`：反转列表元素的排列顺序（永久性修改），与元素大小无关。
- 再次调用 `reverse()` 可恢复原顺序。

#### 确定列表的长度

- `len(list)`：返回列表元素个数（从 1 开始计数）。

### 使用列表时避免索引错误

- 索引超出范围会引发 `IndexError`。
- 如果索引为负，列表为空时也会引发 `IndexError`。
- 调试技巧：打印列表或其长度，确认列表内容。

### 3.5 小结

- 列表是什么及如何访问元素
- 如何增删元素
- 如何对列表进行永久或临时排序
- 如何反转列表及获取长度
- 如何避免索引错误